import Container from "core/UI/Container";
import { SectionWrapper } from "modules/verification/styles";

const DiscordServers = () => {
  return (
    <Container>
      <SectionWrapper style={{ minHeight: "50vh", marginTop: "2rem" }}>
        <h1>Discord servers </h1>
        <p>Some content to fill</p>
      </SectionWrapper>
    </Container>
  );
};

export default DiscordServers;
